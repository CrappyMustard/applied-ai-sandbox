## Bug Hunt 1

1. The bug comes from the fact that the variable "q" is called directly into the SQL string below, which will allow for a attacker to easily run whatever they want inside.
2. The use of f-strings can look natural in Python, with the function using common patterns. If you don't define specific and explicit context when it comes to bound parameters, the AI won't know to include it.
3. Use the aforementioned bound parameters, replacing the one-line "rows" variable.

## Bug Hunt 2

1. "flash_simpleauth" isn't a package that actually exists.
2. "flash_simpleauth" definitely SOUNDS like a real package, and as such the AI would insert it as if it was, but it isn't. Using a command to check dependencies is key to ensuring that the correct packages are used.
3. Replace the fake package with the real package; In this case, it would be "Flask-Login".

## Bug Hunt 3

1. If the list was empty...
2. The AI only tested the 'correct' way in which someone would use it, which while realistically it would work, wouldn't fly in all cases.
3. Add an "if" to address empty queries.

## Bug Hunt 4

1. The key should pretty much NEVER be in the public code that you push out...
2. If you paste a key into an AI, it'll use said key inline due to the literal nature of the AI.
3. Use environment variables alongside a local ".env" file that is included in ".gitignore"; this will ensure better security and still have the code work as intended.

## Bug Fix (4)

For Bug Numero Tres...

def test_empty_average_rate():
     assert average_rating([]) is None

If it returns "None", then that's the bug caught.

## Near-Miss (kind of, anyway)

1. I almost pushed a line of code that seemed to properly address the instructions that Claude and other LLMs should follow in TASK 01, but realized that it was written incorrectly, which would've caused issues.
2. When seeing what I wrote, plugging that into an AI would cause it to misunderstand the concept for the log-in page, changing it into something it is not.
3. The lines as I typed, thinking about what they would mean to an AI.
